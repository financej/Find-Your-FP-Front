import { useState } from "react"
import { useLayoutEffect } from "react"

export default function Redirect({ to, message }) {
    
    const [ isOpen, setIsOpen ] = useState(false)

    useLayoutEffect(() => {
        message ?  
    }, [])

    return (
        <></>
    )
}